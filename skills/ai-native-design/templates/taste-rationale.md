# Deliverable template — artifact + taste rationale + 指纹 check

Fill this for the delivery bundle. Write it as one designer's reasoned work — *show* the judgment,
don't narrate conformance ("taste node here, per the kernel"). Write to files; return paths + a
short summary.

---

## 1 · The artifact
- **What it is:** `<mockups / components / flow / motion spec / design system>`.
- **Form:** de-canvased / code-shaped (`<HTML+CSS / JSX / token file + component specs>`), so it
  is readable, diffable, re-generatable. Path(s): `<...>`.
- **The one feeling it lands:** `<...>`.

## 2 · Design tokens (the machine-enforceable layer of "good")
- Token file: `<path>`. Lint red lines enforced: `<contrast / off-token color / off-scale spacing / …>`.
- What's *deliberately* left to human judgment (soft criteria, not linted): `<...>`.

## 3 · Taste rationale — why this option won
Run the candidate-spread loop; report the judgment, not the pixels.
- **Directions spread:** `<name the genuinely different roads, e.g. editorial / brutalist / warm-handmade / data-dense>` — *directional diversity, not count*.
- **Scorecard verdict** (the option that won, by axis):
  | Axis | This option | Why it beat the others |
  |---|---|---|
  | Empathy / fit | | |
  | Hierarchy | | |
  | Voice | | |
  | Restraint | | |
  | Distinctiveness | | |
  | *(Pacing — motion only)* | | |
- **The converge call (human node):** verdict ∈ `{ship, revise-with-direction, kill-and-respread}` = `<...>`.
  *Who pressed it and the stated reason:* `<one sentence — the discriminating criteria it satisfied, what it refused, who it's for>`.
- **Rejected directions** (so we don't respread into them): `<"tried X, didn't fit, because…">`.

## 4 · 指纹 (fingerprint) / distinctiveness check
- **Fingerprints avoided** (from the six red lines): `<which were at risk; how the artifact stays off the mean>`.
- **True for these people:** `<one sentence — who this is recognizably *theirs* for, the identity not "clean/modern">`.
- **Accepted price:** `<who this is deliberately NOT for — named as a choice, not an oversight>`.

## 5 · What fed back (context becomes infrastructure — must be real edges)
- → decision log: `<the winning rationale entry>`.
- → design system: `<new token / new red line / sharpened soft criterion discovered this round>`.
- → rejected-directions note: `<...>`.
*(If a row is empty, the loop didn't close — the next round will restart from the mean.)*

## 6 · Why this is AI-native & future-leading (2–3 lines, prose, not a scorecard)
`<Drafts were abundant and machine-made; the value was the taste that chose among them. The
design system + decision log are a context moat that compounds — next quarter's first draft lands
closer to your fingerprint automatically, cheaper models widen the spread, and keeping tokens
swappable + the loop fed lets it ride the curves instead of freezing at today's tools.>`

## 7 · Boundary note (only if applicable)
`<If the artifact's core is a human/trust relationship — memorial, crisis-support, child's
first-learning surface — state that AI assisted the abundant work but the human led the expressive
heart, and do not over-claim a fully agentic design.>`
