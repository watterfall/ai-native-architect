# Site Image Assets

These assets are project-bound generated images created with the built-in `image_gen` workflow for this repository.

The final WebP files are saved under `site/assets/images/`, so the site does not reference external temporary generation paths.

See [Visual Assets](../../../docs/visual-assets.md) for source mapping, style constraints, and usage notes.
